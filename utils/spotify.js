const axios = require('axios');

async function getSpotifySong(query) {
  // Placeholder implementation. Replace with real Spotify API calls.
  return `Song info for ${query}`;
}

module.exports = { getSpotifySong };

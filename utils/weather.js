const axios = require('axios');

async function getWeather(location) {
  // Placeholder implementation. Replace with real weather API calls.
  return `Weather info for ${location}`;
}

module.exports = { getWeather };
